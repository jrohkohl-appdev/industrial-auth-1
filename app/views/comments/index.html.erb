<h1>Comments</h1>
<h5 class="mt-0">Comments on posts from users I follow</h5>

<table>
  <thead>
    <tr>
      <th>Author</th>
      <th>Post</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @comments.each do |comment| %>
      <% if comment.author.followers.include?(current_user) || comment.author == current_user %>
        <tr>
          <td><%= comment.author.username %></td>
          <td>
            <%= link_to "Link", photo_path(comment.photo) %>
          </td>
          <td><%= comment.body %></td>
          <% if comment.author == current_user %>
            <td><%= link_to 'Show', comment %></td>
            <td><%= link_to 'Edit', edit_comment_path(comment) %></td>
            <td><%= link_to 'Destroy', comment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          <% end %>
        </tr>
      <% end %>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Comment', new_comment_path %>
